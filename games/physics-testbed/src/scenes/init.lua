local scenes = {
    stacking      = require "scenes.stacking",
    chain         = require "scenes.chain",
    joints        = require "scenes.joints",
    bouncing      = require "scenes.bouncing",
    friction      = require "scenes.friction",
    restitution   = require "scenes.restitution",
    bridge        = require "scenes.bridge",
    ragdoll       = require "scenes.ragdoll",
    particles     = require "scenes.particles",
    playground    = require "scenes.playground",
    cut_the_rope  = require "scenes.cut_the_rope",
    stickman_hook = require "scenes.stickman_hook",
}

return scenes
